import { createMuiTheme, colors } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#ff652d",
    },
    secondary: {
      main: "#ffdf2b",
    },
  },
});

export default theme;
